summary find and replace text in a file or stream using a pattern
flag -i edit the file in place; on a Mac write sed -i '' or it errors
flag -e supply the find-and-replace expression directly on the command line
flag -n suppress default output (only print lines you explicitly request)
